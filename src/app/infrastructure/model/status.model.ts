

export class ProjectStatus {
    id: number;
    description: string;
    visible: boolean;

    constructor(id, description, visible) {
      id = id;
      description = description;
      visible = visible;
    }
}
